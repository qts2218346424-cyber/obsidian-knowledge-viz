# 📱 CoreForge 研核 · 安卓端 (Android) 适配与连线使用指南

> **适用场景**：手机/平板床上刷题、自习室速记公式、图书馆 5G 移动伴学、考研真题与错题随身复盘。  
> 本项目已全面完成移动端响应式布局、触屏交互重构、PWA（渐进式 Web 应用）特性接入以及 Capacitor 原生 APK 编译支持。

---

## 🚀 三种使用方案对比推荐

| 方案 | 推荐指数 | 门槛 | 性能/耗电 | 适用环境 |
| :--- | :---: | :---: | :---: | :--- |
| **方案一：局域网 PWA 桌面快捷方式** | 🌟🌟🌟🌟🌟 **(最强推荐)** | **0门槛**，扫码即用 | 手机 0 耗电，算力全由电脑承担 | 宿舍、家中（同 Wi-Fi 或热点） |
| **方案二：异地穿透 / Tailscale 组网** | 🌟🌟🌟🌟 | 仅需执行一句话命令 | 手机 0 耗电，随时随地 5G 连线 | 外部图书馆、自习室、教室、通勤路上 |
| **方案三：原生 Android APK 打包** | 🌟🌟🌟 | 需配置 Android Studio | 独立客户端包装 | 需要安装 `.apk` 包的特定场景 |

---

## 📱 方案一：局域网一键连线 + PWA 桌面图标（🌟 推荐，3秒搞定）

这是体验最佳、耗电最低的方案。因为 AI 大模型、PDF 题库解析和 Obsidian 知识库索引均在电脑本地运行，手机只负责轻量级界面交互，**手机不仅完全不发热，而且操作极其丝滑**。

### 操作步骤：
1. **启动电脑端**：
   - 在电脑端启动 CoreForge 软件（或在终端运行 `npm run dev:all` / 启动桌面客户端）。
   - 服务端已自动放通 `0.0.0.0` 局域网监听与跨域访问。
2. **获取连线二维码或地址**：
   - 电脑界面右上角点击 **「📱 手机/平板」** 按钮。
   - 弹窗会自动探测并生成当前电脑的局域网 IP 与专属二维码（例如：`http://192.168.1.108:3001`）。
3. **手机扫码或访问**：
   - 手机连接与电脑相同的 **Wi-Fi**（或者电脑连接手机开启的个人热点）。
   - 手机使用**微信、相机或任何手机浏览器**（Chrome、Edge、华为、小米自带浏览器等）扫描该二维码打开。
4. **一键生成桌面 App（添加到主屏幕）**：
   - 在手机浏览器右上角或底部菜单中，点击 **「添加到主屏幕」**（Add to Home Screen）或 **「安装应用」**。
   - 手机桌面上会立即生成 **CoreForge 研核** 的独立 App 图标！
   - 点击图标打开时，**没有任何浏览器顶部地址栏和底部控制栏**，呈现 100% 原生 App 的沉浸式全屏研学体验。

---

## 🌐 方案二：自习室 / 图书馆 5G 异地远程连线

如果您白天去图书馆或自习室学习，电脑放在宿舍，可以通过以下两种方式随时随地用手机 5G 流量连接电脑端：

### 方案 2.1：免费 Cloudflare Tunnel（HTTPS 临时穿透，0配置）
在电脑终端中执行一行命令：
```bash
npx cloudflared tunnel --url http://localhost:3001
```
终端会输出一个全球可达的加密 HTTPS 链接（例如 `https://random-words.trycloudflare.com`）。  
将该链接微信发给手机或扫码打开，在图书馆无论连接任何校园网或 4G/5G，都能实时同步做题和调取电脑知识库！

### 方案 2.2：Tailscale 虚拟组网（固定私有 IP，永不掉线）
1. 在电脑和手机上均安装免费的 **Tailscale** 应用。
2. 两端登录同一个账号，电脑会分配到一个固定的内网 IP（例如 `100.80.30.12`）。
3. 手机在任何网络环境下，直接访问 `http://100.80.30.12:3001` 即可永久直连。

---

## 📦 方案三：打包为独立 Android 原生 APK 安装包

项目根目录下已内置好完整的 `capacitor.config.ts` 配置文件。若您希望编译输出 `.apk` 文件：

### 编译步骤：
1. **安装 Capacitor Android 核心依赖**：
   ```bash
   npm install @capacitor/core @capacitor/cli @capacitor/android
   ```
2. **编译前端静态工程**：
   ```bash
   npm run build:all
   ```
3. **初始化 Android 工程（仅首次需要）**：
   ```bash
   npx cap add android
   ```
4. **同步代码到 Android 工程**：
   ```bash
   npx cap sync android
   ```
5. **在 Android Studio 中打开并导出 APK**：
   ```bash
   npx cap open android
   ```
   在打开的 Android Studio 中，点击顶部菜单：  
   👉 **Build -> Build Bundle(s) / APK(s) -> Build APK(s)**  
   编译完成后点击弹出的 `locate`，即可在 `android/app/build/outputs/apk/debug/` 目录下找到生成的 `app-debug.apk`，传输至手机安装即可！

---

## 🎯 移动端专属优化亮点

1. **移动端底部沉浸 Dock 导航**：
   - 手机端屏幕底部提供 `🏠 首页`、`📝 刷题`、`📚 学习`、`💬 伴学`、`📱 连线` 快捷标签页，大拇指单手触达。
2. **做题中心触屏优化**：
   - 题目选项增大触控受力面积，答题手势灵敏，解析折叠清晰，夜间护眼配色。
3. **研小核桌面伴学宠移动端支持**：
   - 支持移动端触摸拖拽放置，贴边收起，随时提醒背单词与做题进度。
4. **全屏防手势冲突**：
   - 加入了 iOS/Android 沉浸视口 `viewport-fit=cover` 与安全区适配，杜绝页面晃动与拉伸失真。
